// Generated by CoffeeScript 1.3.1
(function() {
  var data, i, markings, obj, options, series, _i, _j;

  data = [];

  data.push([0, 0]);

  data.push([0, 0]);

  for (i = _i = 0; _i <= 100; i = ++_i) {
    data[0].push([i, i + Math.sin(i / 3.14) * 20]);
  }

  for (i = _j = 0; _j <= 100; i = ++_j) {
    data[1].push([i, i + Math.cos(i / 3.14)]);
  }

  markings = [];

  obj = {
    xaxis: {
      from: 10,
      to: 10
    },
    color: "#f4bfbd"
  };

  markings.push(obj);

  series = [];

  series.push({
    data: data[0],
    shadowSize: 0,
    lines: {
      lineWidth: 1
    }
  });

  series.push({
    data: data[1],
    shadowSize: 0,
    lines: {
      lineWidth: 1
    }
  });

  options = {
    grid: {
      borderWidth: 0,
      markings: markings
    },
    xaxis: {
      mode: "time",
      timeformat: "%M:%S"
    }
  };

  $.plot($("#placeholder"), series, options);

}).call(this);
